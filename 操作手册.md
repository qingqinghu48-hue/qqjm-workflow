# 部署操作手册（照着做就行）

> 全程大约 15 分钟，按顺序做，做完一步打一个勾。

> **当前状态：✅ 已全部部署完成（2026-08-08）**

## 第 0 步：创建 GitHub 仓库

1. 浏览器打开 [github.com/new](https://github.com/new)（已登录 qingqinghu48-hue）
2. Repository name 填：`qqjm-workflow`
3. 选择 **Public**（公开）
4. 其他都不用动，点绿色 **Create repository**
5. 看到仓库页面就完成了，**不要**按页面提示创建 README（我们本地已有）

## 第 1 步：本地推送代码

1. 打开文件夹 `C:\Users\Administrator\Documents\Codex\2026-08-07\ke\outputs\deploy`
2. 找到 **setup-and-push.ps1**，右键 → **使用 PowerShell 运行**
3. 弹出窗口提示输入令牌：粘贴你的 `ghp_` 开头的令牌，回车
4. 看到"代码已推送到 GitHub"就成功了
5. 到 GitHub 刷新仓库页面，应该能看到文件列表

## 第 2 步：配置 GitHub 自动部署密钥（Secrets）

1. 打开你的仓库页面：`github.com/qingqinghu48-hue/qqjm-workflow`
2. 点顶部 **Settings** → 左侧 **Secrets and variables** → **Actions**
3. 点绿色 **New repository secret**，添加以下 3 个（一个一个加）：

| Name（名字） | Secret（值） |
| --- | --- |
| `DEPLOY_HOST` | `47.116.116.67` |
| `DEPLOY_USER` | `root` |
| `DEPLOY_PASS` | 你的服务器密码 |

4. 添加完成后，回到仓库页面点 **Actions**，应该能看到一个叫"自动部署到阿里云"的工作流

## 第 3 步：服务器首次安装

1. 打开阿里云控制台 → 云服务器 ECS → 实例
2. 找到 IP `47.116.116.67` 的实例 → 点 **远程连接** → **Workbench 远程连接**（网页会打开一个黑色终端）
3. 登录时输入用户名 `root` 和你的服务器密码
4. 在终端里**逐行**复制粘贴以下 3 条命令，每粘贴一条按一次回车：

```bash
sudo git clone https://github.com/qingqinghu48-hue/qqjm-workflow.git /opt/qqjm
cd /opt/qqjm
sudo bash first_setup.sh
```

5. 看到"部署完成！服务已启动"就成功了
6. 如果第 2 步（Actions）已经配置好，以后本地推送会自动同步到服务器，不用再管

## 第 4 步：验证网站

浏览器打开：`http://47.116.116.67:8080`

- 能看到工作流面板 → 成功
- 打不开 → 检查安全组是否放行 8080 端口（你已完成）；若刚重启实例，等 1 分钟再试

## 日常使用（以后每天）

1. 修改 `deploy` 文件夹里的面板/模板（**注意：以后都改这个文件夹里的文件**）
2. 打开 PowerShell，运行：
   ```powershell
   cd C:\Users\Administrator\Documents\Codex\2026-08-07\ke\outputs\deploy
   .\push.ps1 -m "描述本次修改"
   ```
3. GitHub Actions 自动部署到服务器，约 1 分钟后刷新网页看到新版本

## 常见问题

- **push 时报 403**：令牌过期或权限不够，重新生成令牌（repo 权限）
- **push 报 workflow 拒绝**：令牌必须同时勾选 **repo** 和 **workflow** 两项权限（因为仓库里有自动部署配置）
- **push 报无法连接 github.com**：脚本会自动检测本机代理（内置端口 7078/7890/7897/1080 等），梯子开着就行
- **服务器安装时报错**：把终端里的红色报错截图/复制给 Codex
- **网页打不开**：确认安全组 8080 端口已放行；确认服务器上用 `curl http://127.0.0.1:8080/api/status` 有返回

## 已踩坑记录（给未来的你）

| 坑 | 原因 | 解决 |
| --- | --- | --- |
| bat 双击闪退 | 脚本编码无 BOM / 找不到 Git | 脚本改为 UTF-8 BOM，自动定位 Git |
| Git 报 dubious ownership | 沙箱创建的文件与当前用户不同 | 脚本自动加 safe.directory |
| Git 报 remote-https 不存在 | 内置 Git 的 HTTPS 组件目录不在 PATH | 脚本自动设置 GIT_EXEC_PATH |
| push 报 workflow 拒绝 | 令牌缺 workflow 权限 | 重新生成令牌，勾 repo + workflow |
| push 连不上 GitHub | 国内网络直连不稳定 | 脚本自动走本机代理（7078） |
| Actions 连不上服务器 | 安全组未放行 22 端口 | 安全组入方向放行 22/22 |
